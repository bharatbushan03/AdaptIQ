pluginManagement {
    repositories {
        google()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECTS)
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "Dynamic Behavior-Driven Adaptive UI"
include ":app"